h3.pad= __("Are you sure? You want to rename " + id + "!")

form.partial(method='post', action='/file/' + id + '/rename')
  label
    span= __("New title")
    input.field(type="text", name="title")
  p.centered
    button.btn.danger(type="submit")= __("Rename document")
    span= __(" or ")
    a.close= __("cancel")

