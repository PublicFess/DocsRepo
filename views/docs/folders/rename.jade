h3.pad= __("Are you sure? You want to rename" + " " + title + "!")

form.partial.validate(method='post', action="/folder/" + id + "/rename")
  label
    span= __("New title")
    input.field.focus(type="text", name="title")
  p.centered
    button.btn.danger(type="submit")= __("Rename directory")
    span= __(" or ")
    a.close= __("cancel")

